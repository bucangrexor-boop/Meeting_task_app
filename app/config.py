# app/config.py

# Server URL (replace with your deployed FastAPI server)
SERVER_BASE = "https://yourserver.example.com"

# API token (for authentication)
API_TOKEN = "replace-with-secure-token"

# App version
CURRENT_VERSION = "1.0.0"

# Local database filename
LOCAL_DB = "local.db"
